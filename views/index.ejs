<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Match Hub - Where Champions Connect</title>
    <link rel="stylesheet" href="/css/modern-style.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.3.0/css/all.min.css" />
    <link rel="icon" href="/images/logo.png" />
</head>
<body>
    <nav class="navbar-modern">
        <div class="nav-container">
            <a href="/" class="nav-logo">
                <img src="/images/logo.png" height="40" width="40" style="vertical-align:middle;margin-right:10px;" onerror="this.style.display='none'">
                <span style="font-family:var(--font-display);font-size:1.5rem;letter-spacing:0.05em;">MATCH HUB</span>
            </a>
            <ul class="nav-menu" id="navMenu">
                <li><a href="/" class="nav-link active">Home</a></li>
                <li><a href="#events" class="nav-link">Events</a></li>
                <li><a href="/contactUs" class="nav-link">Contact Us</a></li>
                <% if(!currentUser) { %>
                    <li><a href="/loginUser" class="nav-link">Login</a></li>
                    <li><a href="/registerUser" class="btn btn-primary" style="padding:0.5rem 1.5rem;">Sign Up</a></li>
                <% } %>
                <% if(currentUser) { %>
                    <li><a href="/" class="nav-link"><i class="fas fa-user"></i> <%= currentUser.username || currentUser.email %></a></li>
                    <li><a href="/logout" class="btn btn-accent" style="padding:0.5rem 1.5rem;">Logout</a></li>
                <% } %>
            </ul>
        </div>
    </nav>

        <section class="hero-modern">
        <div class="hero-content">
            <div style="font-size:1rem;font-weight:600;color:rgba(255,255,255,0.9);margin-bottom:0.8rem;letter-spacing:0.2em;text-transform:uppercase;">
                <i class="fas fa-trophy"></i> Elite Sports Platform
            </div>
            <h1 class="hero-title" style="font-family:var(--font-display);font-size:3.5rem;line-height:1;margin-bottom:1rem;text-shadow:2px 4px 8px rgba(0,0,0,0.3);">
                COMPETE.<br/>CONNECT.<br/>CONQUER.
            </h1>
            <% if(!currentUser) { %>
                <p class="hero-subtitle" style="font-size:1.1rem;max-width:650px;margin:0 auto 2rem;font-weight:500;line-height:1.6;">
                    Join thousands of athletes in the ultimate sports arena. 
                    <span style="color:#A6FF00;font-weight:700;">Your next victory starts here.</span>
                </p>
                <div style="display:flex;gap:1rem;justify-content:center;flex-wrap:wrap;">
                    <a href="/loginUser" class="btn btn-white btn-large" style="font-size:1rem;padding:0.8rem 2rem;"><i class="fas fa-bolt"></i> Start Competing</a>
                    <a href="/registerUser" class="btn btn-outline btn-large" style="font-size:1rem;padding:0.8rem 2rem;"><i class="fas fa-rocket"></i> Join The League</a>
                </div>
            <% } %>
            <% if(currentUser) { %>
                <p class="hero-subtitle" style="font-size:1.5rem;font-weight:700;margin-bottom:0.8rem;">
                    Welcome Back, <span style="color:#A6FF00;"><%= currentUser.username || currentUser.email %></span>! 🔥
                </p>
                <p style="font-size:1rem;margin-bottom:1.5rem;font-weight:500;">
                    Your Arena Awaits. Time to Break Records.
                </p>
                <a href="#events" class="btn btn-white btn-large" style="font-size:1rem;padding:0.8rem 2rem;"><i class="fas fa-calendar-star"></i> Browse Matches</a>
            <% } %>
        </div>
    </section>

    <section style="padding:60px 20px;background:#1E1E1E;">
        <div class="section-title">
            <h2 style="font-family:var(--font-heading);font-size:2.5rem;color:#FFFFFF;">Why Choose Match Hub?</h2>
            <p style="font-size:1.1rem;color:#A6FF00;font-weight:600;">Where Athletes Meet Opportunity</p>
        </div>
        <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:2rem;max-width:1200px;margin:2rem auto 0;">
            <div class="feature-card" style="background:#2a2a2a;padding:2rem;border-radius:1rem;box-shadow:0 4px 20px rgba(0,123,255,0.2);text-align:center;transition:all 0.3s;border:1px solid rgba(0,123,255,0.3);">
                <div style="font-size:3rem;color:#00E5FF;margin-bottom:1rem;">
                    <i class="fas fa-calendar-days"></i>
                </div>
                <h3 style="color:#FFFFFF;">Comprehensive Event Listings</h3>
                <p style="color:#A6FF00;">Access all ongoing and upcoming sports events in one place!</p>
            </div>
            <div class="feature-card" style="background:#2a2a2a;padding:2rem;border-radius:1rem;box-shadow:0 4px 20px rgba(0,123,255,0.2);text-align:center;transition:all 0.3s;border:1px solid rgba(0,123,255,0.3);">
                <div style="font-size:3rem;color:#00E5FF;margin-bottom:1rem;">
                    <i class="fas fa-pen-to-square"></i>
                </div>
                <h3 style="color:#FFFFFF;">Real-Time Updates</h3>
                <p style="color:#A6FF00;">Stay informed with dynamic updates about events and registrations.</p>
            </div>
            <div class="feature-card" style="background:#2a2a2a;padding:2rem;border-radius:1rem;box-shadow:0 4px 20px rgba(0,123,255,0.2);text-align:center;transition:all 0.3s;border:1px solid rgba(0,123,255,0.3);">
                <div style="font-size:3rem;color:#00E5FF;margin-bottom:1rem;">
                    <i class="fas fa-address-card"></i>
                </div>
                <h3 style="color:#FFFFFF;">Easy Registration</h3>
                <p style="color:#A6FF00;">Simple and secure event registration process!</p>
            </div>
        </div>
    </section>

    <section class="events-section" id="events">
        <div class="section-title">
            <h2>Featured Events</h2>
            <p>Check out all the ongoing and upcoming events here</p>
        </div>
        <div class="events-grid">
            <% if(event && event.length > 0) { %>
                <% for(var i=0; i<Math.min(3,event.length); i++) { %>
                    <div class="event-card">
                        <img src="<%= event[i].photo || '/images/logo.png' %>" alt="<%= event[i].name %>" class="event-card-image" onerror="this.src='/images/logo.png'" />
                        <div class="event-card-content">
                            <span class="event-card-date"><i class="fas fa-calendar"></i> <%= event[i].datestart ? new Date(event[i].datestart).toLocaleDateString('en-US',{month:'short',day:'numeric',year:'numeric'}) : 'TBA' %></span>
                            <h3 class="event-card-title"><%= event[i].name %></h3>
                            <div class="event-card-details">
                                <div class="event-detail-item"><i class="fas fa-map-marker-alt"></i><span><%= event[i].venue || 'Location TBA' %></span></div>
                                <div class="event-detail-item"><i class="fas fa-users"></i><span><%= event[i].eventcategory || 'Open' %></span></div>
                            </div>
                            <div class="event-card-footer">
                                <div class="event-price"><span class="event-price-label">Entry Fee</span><%= event[i].registerationfee || 0 %></div>
                                <% if(currentUser) { %>
                                    <a href="/fetchdetails/<%= event[i].id %>" class="btn btn-primary">View Details <i class="fas fa-arrow-right"></i></a>
                                <% } else { %>
                                    <a href="/loginUser" class="btn btn-outline">Login to View</a>
                                <% } %>
                            </div>
                        </div>
                    </div>
                <% } %>
            <% } else { %>
                <div class="no-data" style="grid-column:1/-1;">
                    <div class="no-data-icon"><i class="fas fa-calendar-xmark"></i></div>
                    <h3>No Events Available</h3>
                    <p>Please check back later!</p>
                </div>
            <% } %>
        </div>
        <% if(event && event.length > 3) { %>
            <div style="text-align:center;margin:3rem 0;">
                <a href="/checkevents" class="btn btn-accent btn-large">Explore All Events <i class="fas fa-forward"></i></a>
            </div>
        <% } %>
    </section>

    <footer class="footer-modern">
        <div class="footer-content">
            <div class="footer-section">
                <h3 style="font-family:var(--font-display);font-size:1.8rem;letter-spacing:0.05em;">MATCH HUB</h3>
                <p style="color:#e5e7eb;margin-top:1rem;font-size:0.95rem;">Where champions are made. Every match, every moment.</p>
            </div>
            <div class="footer-section">
                <h3>Quick Links</h3>
                <ul class="footer-links">
                    <li><a href="/">Home</a></li>
                    <li><a href="/checkevents">Events</a></li>
                    <li><a href="/contactUs">Contact</a></li>
                </ul>
            </div>
            <div class="footer-section">
                <h3>Connect</h3>
                <div style="display:flex;gap:1rem;margin-top:1rem;">
                    <a href="#" style="color:white;font-size:1.5rem;"><i class="fab fa-instagram"></i></a>
                    <a href="#" style="color:white;font-size:1.5rem;"><i class="fab fa-facebook"></i></a>
                    <a href="#" style="color:white;font-size:1.5rem;"><i class="fab fa-twitter"></i></a>
                </div>
            </div>
        </div>
        <div class="footer-bottom"><p>&copy; 2024 Match Hub. All rights reserved. <span style="color:#fbbf24;">Built for Champions.</span></p></div>
    </footer>

    <script>
        window.addEventListener('scroll',function(){const n=document.querySelector('.navbar-modern');window.scrollY>50?n.classList.add('scrolled'):n.classList.remove('scrolled');});
        document.querySelectorAll('a[href^="#"]').forEach(a=>{a.addEventListener('click',function(e){e.preventDefault();const t=document.querySelector(this.getAttribute('href'));t&&t.scrollIntoView({behavior:'smooth'});});});
    </script>
</body>
</html>
